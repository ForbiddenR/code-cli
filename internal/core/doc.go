// Package core defines normalized contracts shared by the Go rewrite.
//
// These types are intentionally independent from a concrete SDK client so the
// query engine, tools, CLI, and tests can depend on stable message contracts.
package core
